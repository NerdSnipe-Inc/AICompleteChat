import SwiftUI
import DesignFoundation
import DesignFoundationPro
import AIChatCore
import AIChatUI
import AIChatMLX
import AiPersona
import AiVoiceKit

struct ContentView: View {
    var body: some View {
        VStack(spacing: 20) {
            Text("AICompleteChat")
                .font(.title)
                .padding()
        }
    }
}
